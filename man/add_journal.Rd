% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_journal.R
\name{add_journal}
\alias{add_journal}
\title{Add a journal's citation style to the project}
\usage{
add_journal(journal, path = ".", overwrite = FALSE, repo = NULL)
}
\arguments{
\item{journal}{The style's name in the repository, with or without \code{.csl},
or the URL of a \code{.csl} file.}

\item{path}{The project's root. The working directory by default.}

\item{overwrite}{Replace a style already in \verb{references_styles/}. \code{FALSE}
(the default) leaves it and says so.}

\item{repo}{Where to fetch from: the official repository by default.
Another URL, or the path of a local checkout of the repository, to work
offline or behind a mirror.}
}
\value{
The path of the \code{.csl} written, invisibly.
}
\description{
The template ships nine \code{.csl} files, all from ecology. This fetches any
other from the official CSL repository -- a couple of thousand styles, one
per journal or publisher -- and puts it in \verb{references_styles/}, where
\code{render_docx("<journal>")} and \code{make_submission("<journal>")} look for it.
}
\details{
Give the name the repository uses: the file name without \code{.csl}, lower case
with hyphens, as in \code{"nature"}, \code{"plos-one"}, \code{"apa"} or
\code{"journal-of-ecology"}. The list is at
\url{https://github.com/citation-style-language/styles}, and Zotero's style
finder at \url{https://www.zotero.org/styles} searches it by journal name; its
URLs work here too.

Most journals have a \emph{dependent} style: a few lines of metadata pointing at
the parent whose rules they share. Pandoc cannot follow that pointer, so the
parent's rules are fetched and saved under the name you asked for, and the
message says which parent it was.

Nothing new is installed: base R downloads the file, and it is checked to
be a CSL style before it is kept.
}
\examples{
\dontrun{
add_journal("plos-one")            # then: render_docx("plos-one")
add_journal("journal-of-ecology")
add_journal("https://www.zotero.org/styles/nature")
}
}
\seealso{
\code{\link[=update_project]{update_project()}}, which refreshes the styles the template ships
and leaves the ones added this way alone.
}
