% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_project.R
\name{update_project}
\alias{update_project}
\title{Bring a project's build logic up to the installed easypaper}
\usage{
update_project(path = ".", dry_run = FALSE)
}
\arguments{
\item{path}{The project's root, the folder holding \code{make.R}. The working
directory by default, which is the project root once its \code{.Rproj} is
open.}

\item{dry_run}{List the files that would change, and write nothing.}
}
\value{
Invisibly, a data frame with one row per managed file: \code{file}, and
\code{action} -- \code{"update"}, \code{"add"}, \code{"append"} (the \code{.gitignore}) or
\code{"same"}.
}
\description{
A project carries its own copy of the build logic -- \code{make.R}, \code{run.R},
\code{R/submission.R}, the Word templates, the citation styles -- so it renders
with easypaper uninstalled. The price is that a fix in the package does not
reach a project already written. This is how it does: the files are
refreshed from the installed version, and everything you wrote is left
alone.
}
\details{
What it touches is an explicit list, not everything the template has:
\code{make.R}, \code{run.R}, \code{README.md}, \code{data/README.md}, \code{R/submission.R},
\code{R/crossref_styles.R}, \code{R/convert_data.R}, \code{R/renv_setup.R},
\code{R/create_metadata.R}, the Word templates in \verb{format/} and the \code{.csl} files
the template ships in \verb{references_styles/}. Lines the template's
\code{.gitignore} has and yours lacks are appended; nothing is removed from it.

What it never touches: \verb{_sections/}, \code{manuscript.qmd}, \code{supplementary.qmd},
\code{title_page.qmd}, \verb{_quarto.yml}, \verb{references/}, \verb{data/} apart from its
README, \code{R/setup.R}, \code{R/trackdown.R}, the licences, and any \code{.csl} you
added yourself. Nothing is ever deleted.

Two of the managed files are ones you may have edited: \code{R/convert_data.R},
whose \code{.cd_open_formats} list is yours to extend, and \code{make.R}, where a
default journal is easy to change. Both are overwritten, and both are
accounted for: extensions your copy knew and the new one does not are
named so you can put them back, and the project's git history holds the
rest. That is why the function insists on a clean working tree: the update
is then one commit you can read with \verb{git diff} and revert file by file.
In a project with no repository it still runs, and keeps the replaced
files in a folder under \code{tempdir()} until the session ends.

\code{dry_run = TRUE} lists what would change and writes nothing.

Some versions also change what is expected of you: 0.2.0 replaced
\verb{data/raw/} and \verb{data/csv/} with a single \verb{data/}. Those steps involve your
data or your text and are never done for you. They are printed, when the
project needs them, as the list of what is left to do by hand.

The stamp at the top of \code{make.R} then records both versions: the one that
created the project and the one it was updated to.
}
\examples{
dir <- file.path(tempdir(), "ant_chemistry")
create_paper(dir, git = FALSE)
update_project(dir, dry_run = TRUE)   # written by this version: nothing to do
unlink(dir, recursive = TRUE)
}
\seealso{
\code{\link[=create_paper]{create_paper()}}, which writes the project in the first place, and
\code{\link[=add_journal]{add_journal()}} for a citation style the template does not ship.
}
