% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_data.R
\name{convert_data}
\alias{convert_data}
\title{Convert or copy originals into the project's data folder}
\usage{
convert_data(path, to = NULL, overwrite = FALSE, also = character(0))
}
\arguments{
\item{path}{File or folder to read, relative to the working directory --
the project root, when you have opened the project's \code{.Rproj} -- or an
absolute path. A folder is read whole, subfolders included. The originals
are only ever read.}

\item{to}{Where the results go. Defaults to \verb{data/} beside the working
directory.}

\item{overwrite}{\code{FALSE} (the default) writes only what is missing from
\code{to} or older than its original; \code{TRUE} rewrites the lot.}

\item{also}{Extensions to treat as already open for this call, with or
without the dot, e.g. \code{"las"}. Permanently: add them to
\code{.cd_open_formats} at the top of the project's \code{R/convert_data.R}.}
}
\value{
The paths written, invisibly.
}
\description{
The project publishes what is in \verb{data/}: the analysis reads it, the
metadata describes it and the submission compendium carries it. This brings
an original into that folder in a format that will still open in twenty
years. It never writes anywhere else and never touches the original.
}
\details{
Each file takes one of three roads, decided by its extension alone:

converted   \code{.xlsx} and \code{.xls}, one \code{.csv} per sheet (needs readxl);
\code{.sav}, \code{.dta} and \code{.sas7bdat}, one \code{.csv} per file (needs
haven)
copied      anything already open -- plain text, \code{.json}, \code{.parquet},
\code{.nc}, \code{.sqlite}, \code{.gpkg}, a \code{.shp} with its sidecars,
\code{.tif}, \code{.fasta} -- byte for byte, because converting it
would destroy it rather than open it
named       anything else, left where it is and reported on screen

A format conversion, not a transformation: filtering, recoding and cleaning
belong in the analysis chunk that needs them, where a reader can check them.
Every conversion costs something -- an \code{.xlsx} loses its formulas, an \code{.sav}
its value labels -- which is why the original is never moved or altered.

Subfolders of a source folder are read but not reproduced: everything lands
flat in \verb{data/}. When two originals want the same name there, one keeps it
-- whichever comes first in alphabetical order by path -- and the rest are
refused with a warning saying which was kept and which was not, rather than
one table quietly overwriting another.
}
\examples{
\dontrun{
convert_data("originals/counts.xlsx")   # one workbook, one .csv per sheet
convert_data("originals")               # a whole folder
convert_data("~/Downloads/plots.gpkg")  # copied, not converted
convert_data("originals", also = "las") # teach it one more open format
}
}
